<!--
<div class="container-fluid">
    <nav role="navigation" class="navbar navbar-light">
        <div class="navbar-header pull-right">
            {{{usermenu}}}
            <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#moodle-navbar">
                <span class="sr-only">Toggle navigation</span>
                <img src="{{{togglebtn}}}" class="togglebtn" alt="toggle menu">
            </button>
        </div>
        <div id="moodle-navbar" class="navbar-collapse collapse">
            {{{custommenu}}}

            {{{languagemenu}}}

            {{{search}}}
            <ul class="nav pull-right">
                <li>{{{pageheadingmenu}}}</li>
            </ul>
        </div>
    </nav>
</div>
-->

<!-- <header role="banner" class="pos-f-t navbar navbar-full navbar-light bg-faded navbar-static-top moodle-has-zindex"> -->
<header role="banner" class="navbar navbar-full navbar-light bg-faded navbar-static-top moodle-has-zindex">
    <div class="container-fluid navbar-nav">

        <!-- user_menu -->
        {{{ output.user_menu }}}

        <!-- navbar_plugin_output -->
        {{{ output.navbar_plugin_output }}}

        <!-- search_box -->
        <span class="hidden-md-down">
        {{{ output.search_box }}}
        </span>

        <div class="popover-region collapsed popover-region-chsm nav-link">{{{ output.context_header_settings_menu }}}</div>

        <nav class="nav navbar-nav hidden-md-down">
            <!-- custom_menu -->
            {{{ output.custom_menu }}}
            <!-- page_heading_menu -->
            {{{ output.page_heading_menu }}}
        </nav>
    </div>
</header>